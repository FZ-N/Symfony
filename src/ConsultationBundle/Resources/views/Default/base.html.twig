<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Chifa{% endblock %}</title>
        <link rel= "stylesheet"  href="https://bootswatch.com/4/cerulean/bootstrap.css">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
            <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="{{path('homepage')}}">Cabinet Chifa</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">
             <li class="nav-item">
                   
                </li>               
                {% if app.user %}
              <li class="nav-item">
                <a class="nav-link" href="{{path('patient_index')}}">Patients</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('cons_index')}}">Consultations</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('docteur_index')}}">Docteurs</a>
            </li>
             {% endif %}
              {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('user_index')}}">Utilisateurs</a>
                </li>
                
            {% endif %}
            {% if app.user %}
                <right style= "margin-left:600px;" >
                    <a href="{{ logout_path('main') }}" class="btn btn-danger">Se déconnecter</a>
                </right>  {% else %}
                
                     <a href="{{ path('login') }}" style= "margin-left:950px; color:white">SE CONNECTER</a>
                {% endif %}
               
            </ul>
         
        </div>
        </nav>
        
        <div  class ="container">
        {% block body %}

        <center>
        <h1> BIENVENUE </h1>
        <br>
            
            <img src="https://www.le1.ma/wp-content/uploads/2020/03/Zuhr.jpg">
         
           </center>
        {% endblock %}
        </div>
        {% block javascripts %}{% endblock %}
    </body>
</html>
