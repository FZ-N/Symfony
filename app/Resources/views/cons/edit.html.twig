{% extends 'ConsultationBundle:Default:base.html.twig' %}

{% block body %}
    <h1>Modifier la consultation</h1>

    {{ form_start(edit_form) }}
    <table>
    <tr><td>Date</td><td>{{form_widget(edit_form.date)}}</td></tr>
    <tr><td>Heure</td><td>{{form_widget(edit_form.heure)}}</td></tr>
    <tr><td>Remarques</td><td>{{form_widget(edit_form.remarques)}}</td></tr>
    <tr><td>Le patient n'est pas venu au rendez-vous ?</td><td>{{form_widget(edit_form.venu)}}</td></tr>
    {{form_widget(edit_form.patient.setRendered())}}
   {{form_widget(edit_form.doc.setRendered())}}
       <tr><td> <input type="submit" value="Valider" /></td></tr>
    {{ form_end(edit_form) }}
</table>
    <ul>
        
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
