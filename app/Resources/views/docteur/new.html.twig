{% extends 'ConsultationBundle:Default:base.html.twig' %}


{% block body %}
    <h1>Création docteur</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="crée" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('docteur_index') }}">Revenir à la liste</a>
        </li>
    </ul>
{% endblock %}
