{% extends 'ConsultationBundle:Default:base.html.twig' %}

{% block body %}
   <center>
        <br><br>
        <h1> Ajouter un docteur</h1>

        {% form_theme form 'bootstrap_4_layout.html.twig' %}
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" class="btn btn-primary"/>
        {{ form_end(form) }}
        <ul>
            <li>
                <a href="{{ path('docteur_index') }}">Retour vers la liste</a>
            </li>
        </ul>
    </center>
{% endblock %}
